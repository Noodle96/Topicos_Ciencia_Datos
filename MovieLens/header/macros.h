#ifndef MACROS_H
#define MACROS_H

#include <bits/stdc++.h>
using namespace std;

#define all(x) x.begin(),x.end()
#define msg(str,str2) cout << str << str2<< endl
#define DEVELOPING "[Developing] "
#define SOLVE "[Solve] "
#define TAB "\t"

using ll = long long;
using ld = long double;
using uint = unsigned int;
using ull = unsigned long long;
template<typename T>
using pair2 = pair<T, T>;
using pii = pair<int, int>;
using pli = pair<ll, int>;
using pll = pair<ll, ll>;

// Definir tipos de datos rating dataset
using t_userId = int;
using t_movieId = int;
using t_rating = double;
using t_timestamp = string;

#define pb push_back
#define mp make_pair

#endif // MACROS_H
